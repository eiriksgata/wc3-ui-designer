{
  "name": "frame-ui-designer",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "tasks": "node -e \"console.log('可用命令：');console.log('  npm run dev         # 启动网页开发环境');console.log('  npm run build       # 构建网页(dist)');console.log('  npm run preview     # 预览构建结果');console.log('  npm run tauri:dev   # Tauri 开发模式(桌面调试)');console.log('  npm run tauri:build # 打包生成 exe');\"",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "dependencies": {
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@tauri-apps/api": "^2.9.1",
    "@tauri-apps/cli": "^2.9.5",
    "@tauri-apps/plugin-dialog": "^2.4.2",
    "@tauri-apps/plugin-fs": "^2.4.4",
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.0.0"
  }
}
